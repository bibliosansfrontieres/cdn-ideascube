#!/bin/bash

IF=$1
STATUS=$2
PROC=`pgrep kawax`

if [[ "$IF" == "eth0" || "$IF" == "wlan1" ]] && [ -z "$PROC"  ]
then
    case "$STATUS" in
        up)
            /usr/local/bin/kawax all >> /var/log/kawax.log 2>&1
        ;;
        *)
            exit 0;
        ;;
    esac
fi
